# Template only. Do not commit real values.
# Create on the cluster with: kubectl -n agora apply -f k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: agora-secrets
  namespace: agora
type: Opaque
stringData:
  DATABASE_URL: "replace-me"
  SUPABASE_URL: "replace-me"
  SUPABASE_JWT_SECRET: "replace-me"
  OPENAI_API_KEY: "replace-me"
